{
  "name": "api-datamonkey",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^0.12.16"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs -p 8888",
    "build": "openapi bundle -o dist",
    "test": "openapi lint"
  }
}
