type: object
properties:
  code:
    type: integer
    format: int32
    example: 403
  message:
    type: string
    example: "Forbidden: User does not have permission to access this resource"
required:
  - code
  - message
