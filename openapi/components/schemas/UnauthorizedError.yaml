type: object
properties:
  code:
    type: integer
    format: int32
    example: 401
  message:
    type: string
    example: "Unauthorized: Invalid or missing user token"
required:
  - code
  - message
