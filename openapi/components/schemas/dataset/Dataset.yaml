type: object
allOf:
  - $ref: ./DatasetMeta.yaml
  - type: object
    properties:
      id:
        $ref: ../Hash.yaml
      user_token:
        type: string
        description: Token identifying the user who owns this dataset
      created:
        type: string
        format: date-time
      updated:
        type: string
        format: date-time