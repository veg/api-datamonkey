parameters:
  - name: pretty_print
    in: query
    description: Pretty print response
    schema:
      type: boolean
get:
  tags:
    - Jobs
  summary: Get a list of jobs for the authenticated user
  description: Returns jobs owned by the authenticated user, with optional filtering by alignment ID, tree ID, method, and/or status
  operationId: getJobsList
  parameters:
    - name: user_token
      in: header
      required: true
      description: Token identifying the user
      schema:
        type: string
    - name: alignment_id
      in: query
      description: Filter jobs by alignment dataset ID
      schema:
        $ref: ../components/schemas/Hash.yaml
    - name: tree_id
      in: query
      description: Filter jobs by tree dataset ID
      schema:
        $ref: ../components/schemas/Hash.yaml
    - name: method
      in: query
      description: Filter jobs by HyPhy method
      schema:
        type: string
    - name: status
      in: query
      description: Filter jobs by status
      schema:
        type: string
        enum: ["queued", "running", "completed", "error"]
  responses:
    '200':
      description: Success - returns user's jobs
      content:
        application/json:
          schema:
            type: object
            properties:
              jobs:
                type: array
                items:
                  $ref: ../components/schemas/JobStatus.yaml
    '401':
      description: Unauthorized - Invalid or missing user token
      content:
        application/json:
          schema:
            $ref: ../components/schemas/UnauthorizedError.yaml
    '500':
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ServerError.yaml
